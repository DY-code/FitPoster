待改进的地方

- 跑步时长允许精确到秒
- 周数据统计功能
- 目标链绘制
- 跑步图表中额外加入配速曲线（允许和已有曲线同时显示，使用图例和不同的颜色区分不同的曲线），根据每次跑步的时长和路程自动计算（单位为min/km）

- 有时会出现打不开/加载不出的bug（√）
- 添加当前周分隔功能，在图表中本周周一所在的地方绘制一条垂直的虚线（√）
- 为每日行走步数图表绘制10000步标示线（白色虚线），并绘制日均步数统计线（虚线，颜色跟随每日行走步数图表设置）（√）
- 目标模块的内容没有保存，重新打开后目标被重置（√）
- 允许自定义设置图表的最大数据展示量（单位：天），默认为10天（全局设置，对所有图表有效）（√）
- 分项可视化配置支持自定义选择颜色，预览图表中的颜色与壁纸中对应的图表完全一致（√）
- 在壁纸中加入子目标倒计时，在子目标标语前显示，支持用户单独输入日期（√）
- 图片的白色边框再细一点（√）
- 自定义标语：支持导入自定义标语文件（支持自动导入，文件为.txt格式，每一行对应各一个标语，以"- "开头），每次生成壁纸时随机从中挑选一句展示。（√）
- 手动双击打开index.html文件（浏览器加载）时无法使用自动加载数据文件功能（暂时不考虑该功能）
- 设置启动时自动读取指定路径下的数据文件（路径为./data/metadata.json）暂时不考虑该功能）
- 最好能一键设置壁纸（受系统限制该功能暂时没有理想方案，暂时不需要）
- 导出到指定文件路径（浏览器存在限制，暂时不考虑）

---------------------------------------------------
1.15
有时会出现打不开/加载不出的bug


1.14
优化排版功能

1.3
添加功能：
- 添加当前周分隔功能，在图表中本周周一所在的地方绘制一条垂直的虚线
- 为每日行走步数图表绘制10000步标示线（白色虚线），并绘制日均步数统计线（虚线，颜色跟随每日行走步数图表设置）

修复bug：
- 目标模块的内容没有保存，重新打开后目标被重置

1.1
进行中：
为每日行走步数图表绘制10000步标示线（白色虚线），并绘制日均步数统计线（虚线，颜色跟随每日行走步数图表设置）

完成：
- 为图表添加纵坐标
- 允许自定义设置图表的最大数据展示量（单位：天），默认为10天（全局设置，对所有图表有效）
- 分项可视化配置支持自定义选择颜色，预览图表中的颜色与壁纸中对应的图表完全一致


12.30
在数据管理页面添加一个页面内标语管理面板（即多行文本输入区），一行一条标语（约定 - 开头），提供自动保存、导入 / 导出文本功能。启动时自动读取数据，每次生成壁纸时随机从中挑选一句作为励志标语展示。

允许用户自行维护一个自定义标语文件（文件为.txt格式，每一行对应各一个标语，以"- "开头），启动时自动读取该文件，每次生成壁纸时随机从中挑选一句作为励志标语展示。

后续考虑：
访问笔记软件内容，比如flomo
转为桌面应用

- 子目标倒计时设置为在子目标标语前显示

- 设置壁纸的背景图片时，在图片与壁纸尺寸比例不一致的情况下，能允许用户自定义截取图片的指定区域（通过在原始图片范围内拖动与壁纸比例一致的半透明裁剪框实现）作为最终的背景图片
允许调整背景图片的透明度

- 删除不需要的排版功能按键
- 删除壁纸风格选项，并去掉冗余代码

